/**
 * LongBow is a sub type of class Item. One of the items a character can be equipped with during the game.
 * @author Hamid
 */
package Model.Items;

import Model.BodyParts.BodyPart;
import Model.OutputConsole;

public class LongBow extends Weapon
{

    /**
     * Constructor method for LongBow.
     * @param pName
     * @param pFileName 
     */
    public LongBow(String pName, String pFileName)
    {
        _fileName = pFileName;
        _name = pName;
        OutputConsole.GetOutputConsoleInstance().Write("A LongBow was just created.");
    }

    public LongBow()
    {
        _name = "LongBow";
    }

    @Override
    public boolean validateBodyPart(BodyPart pBodyPart)
    {
        //TODO put logic here        
        return true;
    }
}


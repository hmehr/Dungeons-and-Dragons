/**
 * LongSword is a sub type of class Item. One of the items a character can be equipped with during the game.
 * @author Hamid
 */
package Model.Items;

import Model.BodyParts.BodyPart;
import Model.OutputConsole;

public class LongSword extends Weapon
{

    /**
     * Constructor method for LongSword.
     * @param pName
     * @param pFileName 
     */
    public LongSword(String pName, String pFileName)
    {
        _fileName = pFileName;
        _name = pName;
        OutputConsole.GetOutputConsoleInstance().Write("A LongSword was just created.");
    }

    public LongSword()
    {
        _name = "LongSword";
    }

    @Override
    public boolean validateBodyPart(BodyPart pBodyPart)
    {
        //TODO put logic here        
        return true;
    }
}


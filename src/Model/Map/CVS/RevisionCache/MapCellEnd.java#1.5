/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */
package Model.Map;

/**
 * This is the map cell end class. 
 * It inherit the map cell.
 * set the end cell in the map with row and column.
 * 
 * @author yukywang
 */
public class MapCellEnd extends MapCell
{
    /**
     * End map cell to terminate the games.
     * @param pRow row position
     * @param pCol column postion
     * @param pDescription Description
     * @param pImageFile Imaage file with location path.
     */
    public MapCellEnd(int pRow, int pCol, String pDescription, String pImageFile)
    {
        _row = pRow;
        _col = pCol;
        _imageName = pImageFile;
        _description = pDescription;
    }

    /**
     * End map cell to terminate the games.
     * @param pDescription Description
     * @param pImageFile Imaage file with location path.
     */
    public MapCellEnd(String pDescription, String pImageFile)
    {
        _imageName = pImageFile;
        _description = pDescription;
    }

    /**
     * Checks whether the cell has overlap object or not.
     * @return It returns boolean.
     */
    @Override
    public boolean canOverlap()
    {
        return true;
    }
}

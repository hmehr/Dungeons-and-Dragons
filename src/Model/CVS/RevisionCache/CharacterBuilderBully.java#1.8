/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */
package Model;

import Controller.Character.AbilityScoreGeneration;

/**
 * This class implements the <code> CharacterBuilder</code> interface.
 * @author Sayid
 */
public class CharacterBuilderBully implements CharacterBuilder
{
    Character _bullyPlayer;
    AbilityScoreGeneration _asGenerate;
    int[] _abilityScore;

    /**
     * Default constructor, which initializes a new character and the 
     * <code>AbilityScoreGeneration</code> class.
     */
    public CharacterBuilderBully()
    {
        _bullyPlayer = new Character();
        _asGenerate = new AbilityScoreGeneration();
    }
    
    /**
     * <code> buildBodyParts() </code> method will create the parts of the body.
     */
    public void buildBodyParts()
    {
        _bullyPlayer.createBodyParts();
    }

    /** 
     * <code> buildInventoryItems() </code> creates some items and initializes the
     * inventory for the character.
     */
    public void buildInventoryItems()
    {
        _bullyPlayer.createInventoryItems();
    }

    /**
     * This will set the name and image of the characters. And also the character
     * type.
     * @param pName It's the name of character.
     * @param pImagePath Image path/location of the character.
     */
    public void buildAvatar(String pName, String pImagePath)
    {
        _bullyPlayer.setName(pName);
        _bullyPlayer.setAvatar(pImagePath);
        _bullyPlayer.setType(CharacterType.BULLY);
    }

    /**
     * Sets the level of the character.
     * @param pLevel It's the level of the player or monster.
     */
    public void buildLevel(int pLevel)
    {
        _bullyPlayer.setLevel(pLevel);
//        if(pLevel > 0)
//        {
//            for(int i=1; i <= pLevel; i++)
//                _bullyPlayer.addLevel();
//        }
//
//        if(pLevel < 0)
//        {
//            for(int i=1; i <= Math.abs(pLevel); i++)
//                _bullyPlayer.minusLevel();
//        }

    }

    /**
     * Generates the ability score according to the rules and assign to the respective
     * properties in respective order.
     */
    public void buildAbilityScores()
    {
        _abilityScore = _asGenerate.createAbilityList();

        _bullyPlayer.setStrength(_abilityScore[5]);
        _bullyPlayer.setConstitution(_abilityScore[4]);
        _bullyPlayer.setDexterity(_abilityScore[3]);
        _bullyPlayer.setIntelligence(_abilityScore[2]);
        _bullyPlayer.setCharisma(_abilityScore[1]);
        _bullyPlayer.setWisdom(_abilityScore[0]);
    }

    /**
     * It will return the character object.
     * @return <code>character</code> object which is created by the builder.
     */
    public Character getCharacter()
    {
        return _bullyPlayer;
    }


}

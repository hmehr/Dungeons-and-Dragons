/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */
package Model.Map;

import org.junit.After;
import org.junit.AfterClass;
import org.junit.Before;
import org.junit.BeforeClass;
import org.junit.Test;
import static org.junit.Assert.*;

/**
 *
 * @author yukywang
 */
public class MapTest {
    
    public MapTest() {
    }

    @BeforeClass
    public static void setUpClass() throws Exception {
    }

    @AfterClass
    public static void tearDownClass() throws Exception {
    }
    
    @Before
    public void setUp() {
    }
    
    @After
    public void tearDown() {
    }

    /**
     * Test of getMapRows method, of class Map.
     */
    @Test
    public void testGetMapRows() {
        System.out.println("getMapRows");
        Map instance = new Map(1, 2);
        int row = instance.getMapRows();
        assertEquals(row, 1);
    }

    /**
     * Test of getMapCols method, of class Map.
     */
    @Test
    public void testGetMapCols() {
        System.out.println("getMapRows");
        Map instance = new Map(1, 2);
        int col = instance.getMapCols();
        assertEquals(col, 2);
    }

    /**
     * Test of getMapElement method, of class Map.
     */
    @Test
    public void testGetMapElement() {
        System.out.println("getMapElement");
        Map instance = new Map(3,3);
        MapCell ret = instance.getMapElement(1, 1);
        assertNull(ret);

       
    }

    /**
     * Test of addMapElement method, of class Map.
     */
    @Test
    public void testAddMapElement() {
        System.out.println("addMapElement");
        MapCell pMapElement = new MapCellEnd(1, 1, "test1", "test2");
        Map instance = new Map(3, 3);
        boolean result = instance.addMapElement(pMapElement);
        assertTrue(result);

        
    }

    /**
     * Test of removeMapElement method, of class Map.
     */
    @Test
    public void testRemoveMapElement() {
        System.out.println("removeMapElement");
        int pRow = 1;
        int pCol = 2;
        Map instance = new Map(3,3);
        boolean expResult = false;
        boolean result = instance.removeMapElement(pRow, pCol);
        assertEquals(expResult, result);
       
    }

    /**
     * Test of moveMapElement method, of class Map.
     */
    @Test
    public void testMoveMapElement() {
        System.out.println("moveMapElement");
        int pSrcRow = 1;
        int pSrcCol = 2;
        int pDestRow = 2;
        int pDestCol = 3;
        Map instance = new Map(3,3);
        boolean expResult = false;
        boolean result = instance.moveMapElement(pSrcRow, pSrcCol, pDestRow, pDestCol);
        assertEquals(expResult, result);
        
    }

    /**
     * Test of validateMap method, of class Map.
     */
    @Test
    public void testValidateMap() {
        System.out.println("validateMap");
        StringBuffer pMsg = new StringBuffer();
        Map instance = new Map(3,3);
        boolean expResult = false;
        boolean result = instance.validateMap(pMsg);
        assertEquals(expResult, result);
       
    }
}
